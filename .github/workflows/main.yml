name: My First Workflow

on: [push]

env:
  GREETING: Hello
  NAME: Wenxing

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "$GREETING $NAME from GitHub Actions"
        
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script
        run: echo "Hello, GitHub Actions!"
